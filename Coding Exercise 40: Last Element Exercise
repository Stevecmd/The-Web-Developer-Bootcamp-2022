// DEFINE YOUR FUNCTION BELOW:
function lastElement(arr) {
    if(arr.length === 0) {
        return null;
    } else {
        let lastElement = arr[arr.length - 1];
        return lastElement;
    }
}
